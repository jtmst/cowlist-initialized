import React from 'react';
import ReactDOM from 'react-dom';

const CowTable = function () {
  //   console.log(cowList);
  //   return {
  //     /*cowList.forEach( (cowObject) =>
  //             (
  //                 <div> {cowObject.name} </div>
  //             );
  //             )*/
  //   };
};

export default CowTable;
